# contents of pyproject.toml

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "example_package_diego_enry"
version = "0.1.0"
readme = "README.md"
requires-python = ">=3.8"

dependencies = [
  "numpy"
]

[project.optional-dependencies]
test = ["pytest"]
